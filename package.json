{
  "name": "pwarnock.github.io",
  "version": "1.0.0",
  "description": "",
  "main": "postcss.config.js",
  "scripts": {
    "prepare": "husky",
    "build": "hugo --minify",
    "lint:yaml": "find . -name '*.yml' -o -name '*.yaml' | xargs bundle exec yaml-lint",
    "lint:toml": "find . -name '*.toml' | while read file; do python3 -c \"import tomllib; tomllib.load(open('$file'))\" 2>/dev/null || echo \"TOML syntax error in $file\"; done"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@commitlint/cli": "^20.1.0",
    "@commitlint/config-conventional": "^20.0.0",
    "daisyui": "^4.12.10",
    "husky": "^9.1.7",
    "tailwindcss": "^3.4.0"
  },
  "dependencies": {
    "@tailwindcss/typography": "^0.5.19"
  }
}

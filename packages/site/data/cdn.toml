# CDN and Analytics configuration

# Alpine.js is now self-hosted in assets/js/ for better performance and reliability
# See: layouts/partials/head/cdn.html for self-hosted implementation

[[cdns]]
name = "tailwindcss"
url = "https://cdn.tailwindcss.com"
type = "css"
description = "Tailwind CSS CDN (fallback)"

[analytics]
provider = "google"
container_id = "GTM-N9CR6KJ5"
base_url = "https://www.googletagmanager.com"

# Configuration for when to load
[loading]
enabled = true
production_only = false
# Note: Analytics only load in production environment
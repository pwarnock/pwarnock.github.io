{{/* Unified Badge Component - Replaces 68+ hardcoded badge patterns */}}
{{ $variant := .variant | default "default" }}
{{ $size := .size | default "md" }}
{{ $text := .text }}
{{ $class := .class | default "" }}
{{ $ariaLabel := .ariaLabel }}

{{/* Build CSS classes */}}
{{ $classes := slice "badge-system" }}
{{ $classes = $classes | append (printf "badge-system--%s" $variant) }}
{{ $classes = $classes | append (printf "badge-system--%s" $size) }}
{{ $classes = $classes | append $class }}

{{/* Build attributes */}}
{{ $attributes := dict "class" (delimit $classes " ") }}
{{ if $ariaLabel }}
  {{ $attributes = merge $attributes (dict "aria-label" $ariaLabel) }}
{{ end }}

{{/* Render badge */}}
<span {{ range $key, $value := $attributes }}{{ $key }}="{{ $value }}" {{ end }}>
  {{ if $text }}{{ $text }}{{ end }}
  {{ if .slot }}{{ .slot }}{{ end }}
</span>